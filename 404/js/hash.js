function hash(input) {
	var output
	var inputs = {

	}
	Object.prototype.default = get options() {
		return Object.keys(this)
	}
	inputsHandler = input.toLowerCase().replace(/ /g, "").replace("/", "").split(":")
	try {
		switch (inputsHandler.length){
			case 1:
				inputs[inputsHandler[0]]
		}
	}
	catch(err) {

	}
}